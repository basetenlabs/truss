[build-system]
requires = ["flit_core>=3.2"]
build-backend = "flit_core.buildapi"

[project]
name = "transformer-engine"
version = "2.2.0"
description = "Stub transformer-engine for environments without real TE"
requires-python = ">=3.9"
